import styles from "./EmptyPage.module.css";

// Routable and styled, with no content: the EDITEUR tab is out of scope for this build.
export function EditorPage() {
  return <div className={styles.page} />;
}
